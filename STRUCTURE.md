# Directory structure — Cold Case

Canonical layout. Update this file when modules move.

```
coldcase/
├── README.md                       ← pitch + compliance posture + quickstart
├── AGENTS.md                       ← IDE-agnostic agent contract (start here)
├── CLAUDE.md                       ← Claude Code additions
├── codex-instructions.md           ← symlink → CLAUDE.md
├── .cursorrules / .windsurfrules
├── STRUCTURE.md                    ← this file
├── SESSION_STATE.md                ← rolling session handoff
├── project.manifest.json           ← manifest-first metadata
├── .env / .env.example             ← port triplet + providers + ollama + agency letterhead
├── .gitignore                      ← node_modules, dist, tsbuildinfo, .env, uploads
├── dev.sh                          ← one-command bring-up
├── docker-compose.yml              ← mongo + backend + frontend, all bind-mounted
├── Dockerfile.frontend             ← vite dev container
├── index.html
├── package.json                    ← scripts: dev / api / build / check / preview
├── vite.config.ts / tsconfig.json
│
├── .githooks/                      ← tracked git hooks (architecture-drift reminder)
│   └── pre-push
├── scripts/
│   ├── install-git-hooks.sh
│   └── compliance-smoke.sh         ← end-to-end smoke (12 assertions)
│
├── .agent/skills/                  ← repo-local skills (7 core)
├── .agents/workflows/              ← agent workflows (7 core)
├── agents/memory-bank/             ← current-state + invariants + lessons + module-registry
│
├── docs/
│   ├── architecture.md             ← auto-generated by architecture-diff-gate skill
│   ├── comprehensive-prd.md        ← living PRD (versioned changelog)
│   ├── branding.md                 ← colors + logo contract
│   ├── LOCAL_DEVELOPMENT.md
│   ├── adr/                        ← app-level ADRs
│   ├── generated/                  ← auto-generated docs
│   ├── design/
│   │   └── workflow-and-ux.md      ← UX strategy + phased plan + evidence.com data-readiness §13
│   ├── legal/
│   │   ├── sb524-text.md           ← §13663 statute summary
│   │   ├── compliance-status.md    ← city-attorney gap assessment + pilot punch list
│   │   ├── agency-policy-template.md         ← 880-line model policy (city-attorney-adoptable)
│   │   └── vendor-data-handling-clause.md    ← §13663(d) contract clause
│   └── usecase-transcript.txt
│
├── server-py/                      ← FastAPI backend
│   ├── __main__.py
│   ├── server.py                   ← app factory; lifespan wires db + admin seed + audit-chain backfill + tag vocab + retention scheduler
│   ├── requirements.txt / requirements-dev.txt
│   ├── core/
│   │   ├── config.py               ← pydantic-settings
│   │   ├── database.py             ← MongoDB via MongoEngine
│   │   └── dev_auth_bypass.py
│   ├── auth/
│   │   └── app_manifest.py         ← permissions + seed roles (hand-authored)
│   ├── launchpad_admin/            ← RBAC package (verbatim from launchpad-admin-pattern)
│   ├── models/
│   │   ├── case.py                 ← Case + classifications + retention. external_id, agency_ori_snapshot, date_of_incident
│   │   ├── document.py             ← Document pointer (sha256, mime, external_id)
│   │   ├── media_input.py          ← §13663(c)(2) audio/video reference
│   │   ├── conversation.py
│   │   ├── message.py              ← Message with is_first_ai_draft lock
│   │   ├── report.py               ← Report + AIProgram + OfficerSignature + Revisions + evidence_com_* reserved
│   │   ├── approval.py
│   │   ├── audit_event.py          ← Hash-chained: sequence + prev_event_hash + event_hash. 28 event types
│   │   ├── tag.py                  ← Tag (closed vocab) + TagAssignment + Provenance
│   │   ├── person.py               ← Person + Provenance embedded doc (uniform AI lineage)
│   │   ├── timeline_entry.py       ← Detective chronology (manual + AI-extracted)
│   │   ├── note.py                 ← Freeform detective scratch (case / doc / report scope)
│   │   └── vendor_access.py        ← VendorAccessRequest (F10, §13663(d))
│   ├── routers/
│   │   ├── auth.py
│   │   ├── cases.py                ← F1 + ?mine + similar (Jaccard) + system tag computation
│   │   ├── conversations.py        ← F2 + PATCH /messages/{id} 403 first-draft deny
│   │   ├── reports.py              ← F3 promote/sign/export + chain.pdf/diff.pdf
│   │   ├── audit.py                ← F4 + per-case summary + ai-programs + anomalies
│   │   ├── tags.py                 ← Vocabulary + assign + AI suggestions
│   │   ├── persons.py              ← CRUD + AI suggestions + cross-case search + network (2-hop) + mentions
│   │   ├── timeline_entries.py     ← CRUD + AI suggestions
│   │   ├── notes.py                ← Freeform notes CRUD
│   │   ├── next_steps.py           ← State-aware investigative-step suggester
│   │   ├── prompts.py              ← Prompt catalog
│   │   ├── vendor_access.py        ← F10 portal
│   │   ├── admin_retention.py      ← F22 manual sweep trigger
│   │   ├── admin_compliance.py     ← Preflight (7 checks) + audit-chain verify/rechain
│   │   └── _deps.py                ← CurrentUser, current_user, require_perm
│   ├── services/
│   │   ├── audit_chain.py          ← Hash chain: compute / insert_chained / verify / backfill / rechain
│   │   ├── case_audit.py           ← Domain audit logger (routes through audit_chain.insert_chained)
│   │   ├── audit.py                ← Admin-pattern audit (RBAC changes)
│   │   ├── retention_scheduler.py  ← Daily background sweeper task
│   │   ├── retention_sweeper.py    ← §13663(b) first-draft floor preservation
│   │   ├── external_id.py          ← Federated id composer ({ori}:{case_number}:doc:{id}…)
│   │   ├── seed_defaults.py        ← Lifespan seed: admin role, demo case, tag vocab, audit-chain backfill, external_id backfill
│   │   ├── ollama_client.py
│   │   ├── admin_assistant.py
│   │   ├── artifact_store.py       ← Local-fs storage seam
│   │   ├── chain_export.py         ← F7 chain-of-custody PDF
│   │   ├── case_manifest_export.py ← F15 per-case audit manifest PDF
│   │   ├── diff_export.py          ← F9 officer's-editorial-work diff PDF
│   │   ├── discovery_package.py    ← F8 discovery ZIP
│   │   ├── document_text.py        ← Extraction (text layer / OCR fallback)
│   │   ├── report_export.py        ← F3 §13663(a)(1) PDF stamping
│   │   └── vendor_scope.py         ← F20 vendor scope runtime gate
│   ├── providers/
│   │   ├── llm.py                  ← Ollama (mock) + OpenAI + GccCopilot (stub)
│   │   ├── document_storage.py     ← Local-fs mock + Azure Blob stub
│   │   └── mock_*.py
│   ├── seed/
│   │   ├── synthetic_case.py       ← 1992 Riverside Park demo
│   │   └── civil_rights_cases.py   ← 3 real 1945 federal investigative files
│   ├── lib/
│   │   └── hash.py
│   └── tests/
│
└── src/                            ← React frontend
    ├── main.tsx                    ← QueryClient + UserContextProvider + configureAdminApi
    ├── App.tsx                     ← AppRouter
    ├── index.css                   ← Darwin tokens — element rules wrapped in @layer base (Tailwind v4 cascade fix)
    ├── shell/
    │   ├── AppRouter.tsx           ← Hash-routed page switch
    │   ├── AppShell.tsx            ← Collapsible left rail + topbar (single brand mark)
    │   ├── routes.ts               ← Routes + helpers + parseHashQuery + reportRoute
    │   ├── breadcrumbTrail.ts      ← Uses ShellChromeContext.detailLabel for case number
    │   ├── ShellChromeContext.tsx
    │   └── useHashRoute.ts
    ├── launchpad-admin/            ← RBAC UI (verbatim package)
    ├── components/                 ← Darwin primitives + ImpersonationBanner
    ├── lib/api/
    │   ├── client.ts               ← axios + interceptors
    │   ├── coldcase.ts             ← Typed API surface for every endpoint
    │   ├── mappers.ts / types.ts
    └── features/
        ├── admin/
        │   ├── pages/AdminPage.tsx
        │   └── components/CompliancePreflightCard.tsx
        ├── dashboard/
        │   └── pages/DashboardPage.tsx
        ├── audit/
        │   └── pages/AuditPage.tsx
        └── cases/
            ├── pages/
            │   ├── CaseListPage.tsx           ← Search + class filter + tag filter + show-fixtures toggle
            │   ├── CaseDetailPage.tsx         ← Tabbed workspace orchestrator (7 tabs + chat) — under 1k lines after Brief/People/Timeline/Chain extraction
            │   └── ReportWorkspacePage.tsx    ← Full-route report editor
            ├── tabs/
            │   ├── BriefTab.tsx               ← Stat cards · next-steps suggester · key dates · investigators · connections · similar cases · tags · notes · identifiers
            │   ├── PeopleTab.tsx              ← Role-grouped persons + AI extraction suggester + cross-case lookup + document mention finder
            │   ├── TimelineTab.tsx            ← Case events (manual + AI) · activity log (audit events) · exports `timelineColorFor` reused by ChainTab
            │   └── ChainTab.tsx               ← Chain-integrity card · per-report chain · case-wide audit manifest PDF
            ├── components/
            │   ├── ChatPanel.tsx              ← Per-assistant-message tag bar
            │   ├── CitationText.tsx           ← Clickable [src: …] chips
            │   ├── ReportDrawer.tsx           ← Promote flow + ReportWorkspace (exported)
            │   ├── TagChips.tsx               ← TagChip + CaseTagBar + portaled picker
            │   └── NotesPanel.tsx             ← Scope-agnostic notes (case/doc/report)
            ├── index.ts
            └── queryKeys.ts
```

## Module ownership — where to look first

| Concern | File |
|---|---|
| Add a compliance check | `server-py/routers/admin_compliance.py` |
| Add an AI suggestion endpoint | `server-py/routers/{tags,persons,timeline_entries,next_steps}.py` — same template |
| Add a new audit-event type | `server-py/models/audit_event.py` — append to enum, no other change needed |
| Add a tab to the case workspace | `src/features/cases/pages/CaseDetailPage.tsx` — `CaseTab` type + `CASE_TABS` array + tab content render. Drop the implementation in `src/features/cases/tabs/<Tab>Tab.tsx` and import it. |
| Add an artifact with AI provenance | Reuse `Provenance` embedded doc from `server-py/models/person.py` |
| Add a derived graph query | `server-py/routers/persons.py` (cross-case) or `server-py/routers/cases.py` (similar) |
| Change Tailwind globals | `src/index.css` — keep element rules **inside `@layer base`** (kit cascade fix) |
| Add demo / seed data | `server-py/seed/` |
