# Directory structure — Cold Case

Canonical layout. Update this file when modules move.

```
coldcase/
├── AGENTS.md                       ← IDE-agnostic entry point (start here)
├── CLAUDE.md                       ← Claude Code additions (architecture map)
├── codex-instructions.md           ← symlink → CLAUDE.md
├── .cursorrules                    ← Cursor entry
├── .windsurfrules                  ← Windsurf entry
├── README.md                       ← getting started
├── STRUCTURE.md                    ← this file
├── SESSION_STATE.md                ← rolling session handoff
├── project.manifest.json           ← manifest-first metadata
├── .env.example                    ← env template (port triplet + providers + ollama)
├── .gitignore
├── dev.sh                          ← one-command bring-up
├── docker-compose.yml              ← mongo (named volume)
├── index.html
├── package.json                    ← scripts: dev / api / build / check / preview
├── vite.config.ts
├── tsconfig.json
│
├── .githooks/                      ← tracked git hooks (architecture drift reminder)
│   └── pre-push
├── scripts/
│   └── install-git-hooks.sh        ← wires core.hooksPath
│
├── .agent/skills/                  ← repo-local skills (7 core)
├── .agents/workflows/              ← agent workflows (7 core)
├── agents/memory-bank/             ← current-state + invariants + lessons + module-registry
│
├── docs/
│   ├── architecture.md             ← auto-generated by architecture-diff-gate skill
│   ├── comprehensive-prd.md        ← living PRD
│   ├── branding.md                 ← colors + logo contract
│   ├── LOCAL_DEVELOPMENT.md
│   ├── adr/                        ← app-level ADRs
│   └── generated/                  ← auto-generated docs
│
├── server-py/                      ← FastAPI backend
│   ├── __main__.py
│   ├── server.py                   ← app factory (create_app + lifespan)
│   ├── requirements.txt
│   ├── requirements-dev.txt
│   ├── requirements-local.txt      ← editable installs for local darwin-* clones (optional)
│   ├── core/
│   │   ├── config.py               ← pydantic-settings
│   │   ├── database.py             ← MongoDB via MongoEngine
│   │   └── dev_auth_bypass.py
│   ├── auth/
│   │   └── app_manifest.py         ← hand-authored (only hand-authored admin file)
│   ├── launchpad_admin/            ← RBAC package (8 files, verbatim from launchpad-admin-pattern)
│   ├── routers/
│   │   └── auth.py
│   ├── models/
│   ├── services/
│   │   ├── ollama_client.py        ← shared JSON-mode wrapper
│   │   ├── admin_assistant.py      ← proposer+reviewer
│   │   ├── audit.py                ← fire-and-forget audit log
│   │   └── seed_defaults.py        ← dev admin assignment
│   ├── providers/
│   │   ├── __init__.py             ← factory
│   │   ├── base.py                 ← abstract interfaces
│   │   └── mock_*.py               ← six mocks (employee/email/calendar/training/evaluation/photos)
│   ├── lib/
│   └── tests/
│
└── src/                            ← React frontend
    ├── main.tsx                    ← QueryClient + UserContextProvider + configureAdminApi
    ├── App.tsx                     ← AppRouter
    ├── index.css                   ← Darwin design tokens (P5)
    ├── types/
    │   └── index.ts
    ├── shell/
    │   ├── AppRouter.tsx
    │   ├── AppShell.tsx            ← collapsible left rail + top bar (P3)
    │   ├── routes.ts               ← hash routes + is<X>Route helpers
    │   ├── breadcrumbTrail.ts
    │   ├── ShellChromeContext.tsx  ← context for breadcrumb display labels + nav collapse
    │   └── useHashRoute.ts
    ├── launchpad-admin/            ← RBAC UI package (14 files, verbatim)
    ├── components/                 ← Darwin primitives + ImpersonationBanner
    ├── lib/
    │   ├── api/
    │   │   ├── client.ts           ← axios + interceptors (impersonation, detail→message)
    │   │   ├── mappers.ts
    │   │   └── types.ts
    │   └── queryKeys.ts
    └── features/
        ├── admin/                  ← AdminPage route wrapper
        └── index.ts                ← barrel
```
