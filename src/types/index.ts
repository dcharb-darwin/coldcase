/**
 * Shared TypeScript interfaces for Cold Case.
 * Add domain types here as features land. Never use `any` — see docs/CONVENTIONS.md.
 */

// Replace this stub with your domain entities.
// Example:
//   export interface Employee {
//     id: string;
//     firstName: string;
//     lastName: string;
//     department: string;
//   }

export type Placeholder = unknown;
