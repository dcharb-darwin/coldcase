# Cold Case — Python dependencies
# Standalone FastAPI (no darwin-resty / darwin-common for POC)

fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
mongoengine>=0.29.0,<1.0.0
pydantic[email]>=2.0.0,<3.0.0
email-validator>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
httpx>=0.28.0,<1.0.0
jinja2>=3.1.0,<4.0.0
reportlab>=4.0.0,<5.0.0
pypdf>=4.0.0,<7.0.0
# OCR fallback for image-only scanned PDFs (sibling pattern from ada).
# Requires the `tesseract` binary on PATH (apt-get install tesseract-ocr).
pymupdf>=1.24,<2.0
pytesseract>=0.3.10,<0.4
Pillow>=10,<12
# Graph layer: derived multi-dimensional graph view over Mongo
# (per docs/design/graph-layer.md). Default in-memory backend uses
# NetworkX. Swappable behind GraphService Protocol once a tenant
# outgrows the in-memory ceiling.
networkx>=3.2,<4
