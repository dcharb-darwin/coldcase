# Architecture — Cold Case

> Auto-generated by `architecture-diff-gate` skill. Do not hand-edit.
> Generated from `coldcase/` working tree; the skill inventories
> routers, models, services, features, shell routes, and compose services.

## Identity

- **Display name:** Cold Case
- **Slug:** `coldcase`
- **Description:** Cold case investigation, lead management, and evidence tracking for law enforcement

## Ports

- `BACKEND_PORT` = **7787**
- `FRONTEND_PORT` = **5178**
- `MONGO_PORT` = **27022**

## Backend routers

| Module | Prefix | Endpoints |
|---|---|---|
| `auth` | `/auth` | 1 |

### Router mount order (from `server.py`)

1. `auth_router`
2. `employees_router`
3. `build_admin_router`

## Service modules

- `services/admin_assistant.py`
- `services/audit.py`
- `services/ollama_client.py`
- `services/seed_defaults.py`

## Frontend features

- **admin** — pages: `AdminPage`

## Frontend routes

| Key | Path |
|---|---|
| `dashboard` | `/` |
| `admin` | `/admin` |

## docker-compose services

- `mongo`

## Data flow

Browser (Vite dev / built SPA)
  → Axios client in `src/lib/api/` — base URL `/launchpad/{slug}/api`
  → Vite proxy / deploy-time reverse proxy
  → FastAPI app (`server.py::create_app`)
    → Launchpad Admin middleware (attaches UserContext)
    → Domain routers (see table above)
    → Services (business logic)
    → MongoEngine → MongoDB

External providers (email, calendar, training, eval, photos) are
swappable via `PROVIDER_*` env vars. Mock today; real per-tenant.
